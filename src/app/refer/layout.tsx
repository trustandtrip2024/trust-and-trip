// Metadata for /refer (client-rendered).

export const metadata = {
  title: "Refer & Earn",
  description: "Share Trust and Trip with friends. Earn travel credits when they book a trip.",
  alternates: { canonical: "https://trustandtrip.com/refer" },
};

export default function ReferLayout({ children }: { children: React.ReactNode }) {
  return children;
}
