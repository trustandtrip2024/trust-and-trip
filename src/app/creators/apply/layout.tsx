// Metadata for /creators/apply (client-rendered).

export const metadata = {
  title: "Become a Trust and Trip creator",
  description: "Apply to the Trust and Trip Creator Program. Earn commission on every traveller you refer.",
  alternates: { canonical: "https://trustandtrip.com/creators/apply" },
};

export default function CreatorsApplyLayout({ children }: { children: React.ReactNode }) {
  return children;
}
